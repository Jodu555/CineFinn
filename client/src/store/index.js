import { createStore } from 'vuex'

import watch from '@/store/watch.store';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    watch
  }
})
